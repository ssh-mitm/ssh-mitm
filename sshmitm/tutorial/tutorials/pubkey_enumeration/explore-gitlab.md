<div class="scenario-box" markdown="1">
The intercepted command pointed to an internal Git platform — **LogfileGit**. It's accessible at [{git_server_url}]({git_server_url}) and exposes registered SSH keys on every user profile, just like GitHub's `/username.keys` endpoint.
</div>

<div class="task-box" markdown="1">
Open **[{git_server_url}]({git_server_url})** in your browser. Navigate to **mmorgan's profile** and find their registered SSH public keys.

When you have found them, click **Continue**.
</div>
