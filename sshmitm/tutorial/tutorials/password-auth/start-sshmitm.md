## Start SSH-MITM

Run the following command in **another terminal**.

SSH-MITM listens on port **{sshmitm_port}** and forwards all connections
transparently to the mock server on port **{mock_port}**.
