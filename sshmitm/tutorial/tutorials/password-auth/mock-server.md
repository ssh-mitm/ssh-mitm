## Mock Server

A mock SSH server has been configured as the **target** for this tutorial.

It acts as the destination SSH server that your client ultimately connects to.
SSH-MITM will sit between client and server, forwarding all traffic
while silently intercepting credentials.
