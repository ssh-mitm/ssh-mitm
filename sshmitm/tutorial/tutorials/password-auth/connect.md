## Connect via SSH

Connect as **{password_user}** through SSH-MITM on port **{sshmitm_port}**.

When the SSH client asks for a password, enter the one shown below.

SSH-MITM intercepts and logs the password **before** forwarding it —
even though the SSH connection itself is fully encrypted.
